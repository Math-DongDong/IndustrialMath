# Minimal requirements for the Streamlit app
# only top-level packages imported in the workspace should be listed here.
# Dependencies of these packages will be resolved automatically by pip.

streamlit==1.54.0
pandas==2.3.0
numpy==2.3.0
matplotlib==3.10.8
ripser==0.6.14
persim==0.3.8
ortools==9.15.6755
